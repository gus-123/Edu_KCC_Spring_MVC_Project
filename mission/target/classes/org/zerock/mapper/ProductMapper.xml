<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.zerock.mapper.ProductMapper">
	
	<insert id="productAdd">
	
	<selectKey keyProperty="pno" order="BEFORE" resultType="long">
		select product_seq.nextval from dual
	</selectKey >
	
		insert into product(pno, pname, pimage, price) values(#{pno}, #{pname}, #{pimage}, #{price})
	</insert>
	
	<select id="getList" resultType="org.zerock.domain.ProductVO">
		select * from product 
		<where>
		<![CDATA[
			pno > 0
		]]>
		</where>
		order by pno asc
	</select>
	

</mapper>